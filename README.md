This project is a small rpg game.

It has two modes. The first mode is a game with mobs. 
The second mode is a game with another player.

To run a project execute following command in a project directory:
```sh
./gradlew run
```